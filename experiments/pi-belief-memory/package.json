{
	"name": "pi-belief-memory",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Belief-graph short-term memory for the Pi coding agent: keeps a verbatim recent tail, represents older context as an EpiGraph-style belief graph.",
	"main": "src/index.ts",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"peerDependencies": {
		"@earendil-works/pi-ai": "*",
		"@earendil-works/pi-coding-agent": "*"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"typescript": "^5.7.0",
		"vitest": "^2.1.0"
	}
}
